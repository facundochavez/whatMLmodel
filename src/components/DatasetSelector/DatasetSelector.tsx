import { DatasetSelectorProps } from '../TablesGroup/types';

const DatasetSelector: React.FC<DatasetSelectorProps> = ({
  similarDatasets,
}) => {
  return <header className='flex'>

  </header>;
};

export default DatasetSelector;
